name: tofu

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"
  workflow_dispatch:
    inputs:
      extra_args_gcloud:
        description: |-
          Extra flags for the GCloud Tofu apply/plan

          Often used:
          -replace=module.gcloud_vm.google_compute_instance.vm_instance
        required: false
        default: ""
      extra_args_shared:
        description: |-
          Extra flags for the shared Tofu apply/plan
        required: false
        default: ""
      extra_args_cloudflare:
        description: |-
          Extra flags for the Cloudflare Tofu apply/plan
        required: false
        default: ""
  schedule:
    - cron: "0 4 * * *"

permissions:
  pull-requests: write
  contents: read

concurrency:
  group: ${{ github.workflow }}

jobs:
  gcloud:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: opentofu/setup-opentofu@v2
      - run: |
          echo '${{ secrets.GOOGLE_CREDENTIALS }}' > /dev/shm/credentials.json
      - env:
          GOOGLE_APPLICATION_CREDENTIALS: /dev/shm/credentials.json
          PG_CONN_STR: ${{ secrets.PG_CONN_STR }}
          TF_VAR_project_id: ${{ secrets.GOOGLE_PROJECT_ID }}
          TF_VAR_discord_webhook: ${{ secrets.DISCORD_WEBHOOK_SYSTEMS }}
          TF_VAR_ssh_public_keys: ${{ secrets.SSH_PUB_KEY }}
          TF_VAR_tailscale_oauth_client_id: ${{ secrets.TAILSCALE_OAUTH_CLIENT_ID }}
          TF_VAR_tailscale_oauth_secret: ${{ secrets.TAILSCALE_OAUTH_SECRET }}
          TF_VAR_tailscale_tailnet: ${{ secrets.TAILSCALE_TAILNET }}
        run: |
          source ./hack/actions.sh
          retry 1 tofu -chdir=./terraform/gcloud init -upgrade
      - if: github.event_name == 'pull_request'
        id: plan
        env:
          GOOGLE_APPLICATION_CREDENTIALS: /dev/shm/credentials.json
          PG_CONN_STR: ${{ secrets.PG_CONN_STR }}
          TF_VAR_project_id: ${{ secrets.GOOGLE_PROJECT_ID }}
          TF_VAR_discord_webhook: ${{ secrets.DISCORD_WEBHOOK_SYSTEMS }}
          TF_VAR_ssh_public_keys: ${{ secrets.SSH_PUB_KEY }}
          TF_VAR_tailscale_oauth_client_id: ${{ secrets.TAILSCALE_OAUTH_CLIENT_ID }}
          TF_VAR_tailscale_oauth_secret: ${{ secrets.TAILSCALE_OAUTH_SECRET }}
          TF_VAR_tailscale_tailnet: ${{ secrets.TAILSCALE_TAILNET }}
        run: |
          ARGS="${{ github.event.inputs.extra_args_gcloud }}"
          source ./hack/actions.sh
          retry 1 tofu -chdir=./terraform/gcloud plan -input=false -no-color \
            ${ARGS:+$ARGS}
      - if: github.event_name != 'pull_request'
        env:
          GOOGLE_APPLICATION_CREDENTIALS: /dev/shm/credentials.json
          PG_CONN_STR: ${{ secrets.PG_CONN_STR }}
          TF_VAR_project_id: ${{ secrets.GOOGLE_PROJECT_ID }}
          TF_VAR_discord_webhook: ${{ secrets.DISCORD_WEBHOOK_SYSTEMS }}
          TF_VAR_ssh_public_keys: ${{ secrets.SSH_PUB_KEY }}
          TF_VAR_tailscale_oauth_client_id: ${{ secrets.TAILSCALE_OAUTH_CLIENT_ID }}
          TF_VAR_tailscale_oauth_secret: ${{ secrets.TAILSCALE_OAUTH_SECRET }}
          TF_VAR_tailscale_tailnet: ${{ secrets.TAILSCALE_TAILNET }}
        run: |
          ARGS="${{ github.event.inputs.extra_args_gcloud }}"
          source ./hack/actions.sh
          retry 2 tofu -chdir=./terraform/gcloud \
            apply -auto-approve -input=false -lock=true -no-color \
            ${ARGS:+$ARGS}
      - if: always()
        run: |
          rm -f /dev/shm/credentials.json

  # shared:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v6
  #     - uses: opentofu/setup-opentofu@v2
  #     - env:
  #         PG_CONN_STR: ${{ secrets.PG_CONN_STR }}
  #         TF_VAR_cloudflare_api_key: ${{ secrets.CLOUDFLARE_API_KEY }}
  #         TF_VAR_bws_access_token: ${{ secrets.BW_ACCESS_TOKEN }}
  #       run: |
  #         source ./hack/actions.sh
  #         retry 1 tofu -chdir=./terraform/shared init -upgrade
  #     - if: github.event_name == 'pull_request'
  #       id: plan
  #       env:
  #         PG_CONN_STR: ${{ secrets.PG_CONN_STR }}
  #         TF_VAR_cloudflare_api_key: ${{ secrets.CLOUDFLARE_API_KEY }}
  #         TF_VAR_bws_access_token: ${{ secrets.BW_ACCESS_TOKEN }}
  #       run: |
  #         ARGS="${{ github.event.inputs.extra_args_shared }}"
  #         source ./hack/actions.sh
  #         retry 1 tofu -chdir=./terraform/shared plan -input=false -no-color \
  #           ${ARGS:+$ARGS}
  #     - if: github.event_name != 'pull_request'
  #       env:
  #         PG_CONN_STR: ${{ secrets.PG_CONN_STR }}
  #         TF_VAR_cloudflare_api_key: ${{ secrets.CLOUDFLARE_API_KEY }}
  #         TF_VAR_bws_access_token: ${{ secrets.BW_ACCESS_TOKEN }}
  #       run: |
  #         ARGS="${{ github.event.inputs.extra_args_shared }}"
  #         source ./hack/actions.sh
  #         retry 2 tofu -chdir=./terraform/shared \
  #           apply -auto-approve -input=false -lock=true -no-color \
  #           ${ARGS:+$ARGS}

  # cloudflare:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v6
  #     - uses: opentofu/setup-opentofu@v1
  #     - env:
  #         PG_CONN_STR: ${{ secrets.PG_CONN_STR }}
  #         TF_VAR_cloudflare_api_key: ${{ secrets.CLOUDFLARE_API_KEY }}
  #         TF_VAR_cloudflare_account_id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  #       run: |
  #         source ./hack/actions.sh
  #         retry 1 tofu -chdir=./terraform/cloudflare init -upgrade
  #     - if: github.event_name == 'pull_request'
  #       id: plan
  #       env:
  #         PG_CONN_STR: ${{ secrets.PG_CONN_STR }}
  #         TF_VAR_cloudflare_api_key: ${{ secrets.CLOUDFLARE_API_KEY }}
  #         TF_VAR_cloudflare_account_id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  #       run: |
  #         ARGS="${{ github.event.inputs.extra_args_cloudflare }}"
  #         source ./hack/actions.sh
  #         retry 1 tofu -chdir=./terraform/cloudflare plan -input=false -no-color \
  #           ${ARGS:+$ARGS}
  #     - if: github.event_name != 'pull_request'
  #       env:
  #         PG_CONN_STR: ${{ secrets.PG_CONN_STR }}
  #         TF_VAR_cloudflare_api_key: ${{ secrets.CLOUDFLARE_API_KEY }}
  #         TF_VAR_cloudflare_account_id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  #       run: |
  #         ARGS="${{ github.event.inputs.extra_args_cloudflare }}"
  #         source ./hack/actions.sh
  #         retry 2 tofu -chdir=./terraform/cloudflare \
  #           apply -auto-approve -input=false -lock=true -no-color \
  #           ${ARGS:+$ARGS}
